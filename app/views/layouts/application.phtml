<html lang="pt-br">

<head>
    <?php require '_head.phtml'; ?>
    <title>Calorize</title>

    <script src="/assets/js/application.js"></script>
    <script src="/assets/js/modal.js"></script>
    <script src="/assets/js/foodUnit.js"></script>
    <script src="/assets/js/food_preview.js"></script>


    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;600&family=DM+Serif+Display&display=swap" rel="stylesheet">

    <style>
        body { font-family: 'DM Sans', sans-serif; }
        .nav-link-active { color: #098A26; border-left: 3px solid #098A26; background: #f0faf2; }
        .nav-link:hover { color: #098A26; background: #f0faf2; }
    </style>
</head>

<body class="mt-15 bg-gray-50">
    <nav class="fixed top-0 left-0 right-0 z-40 bg-white border-b border-black/10 h-15 flex items-center px-4">
        <button id="menu-toggle" class="lg:hidden p-1 text-[#098A26]">
            <i class="bi bi-list text-xl"></i>
        </button>
        
        <a href="<?= $this->current_user->is_admin ? route('admin.index') : route('root') ?>" class="hidden lg:!flex">
            <img src="/assets/images/defaults/logo.svg" class="h-8 w-auto" alt="Calorize">
        </a>

        <div class="hidden lg:!flex flex-1 justify-center items-center gap-10">
            <a href="<?= route('root') ?>"
            class="text-md font-medium border-b-2 transition-colors <?= $_SERVER['REQUEST_URI'] === '/' ? 'border-primary text-primary' : 'border-transparent text-black hover:text-gray-600' ?>">
                Início
            </a>
            <a href="<?= route('diets.index') ?>"
            class="text-md font-medium border-b-2 transition-colors <?= str_starts_with($_SERVER['REQUEST_URI'], '/diets') ? 'border-primary text-primary' : 'border-transparent text-black hover:text-gray-600' ?>">
                Minhas Dietas
            </a>
            <a href="<?= route('profile.foods.index') ?>"
            class="text-md font-medium border-b-2 transition-colors <?= str_starts_with($_SERVER['REQUEST_URI'], '/profile/foods') ? 'border-primary text-primary' : 'border-transparent text-black hover:text-gray-600' ?>">
                Meus Alimentos
            </a>

            <a href="<?= route('profile.show') ?>"
            class="text-md font-medium border-b-2 transition-colors <?= $_SERVER['REQUEST_URI'] === '/profile' || (str_starts_with($_SERVER['REQUEST_URI'], '/profile') && !str_starts_with($_SERVER['REQUEST_URI'], '/profile/foods')) ? 'border-primary text-primary' : 'border-transparent text-black hover:text-gray-600' ?>">
                Perfil
            </a>
        </div>

        <?php if ($this->current_user) : ?>
            <details class="ml-auto relative group">
                <summary class="flex items-center gap-1 text-sm text-gray-700 hover:text-gray-900 cursor-pointer list-none">
                    <span class="font-medium"><?= $this->current_user->email ?></span>
                    <i class="bi bi-chevron-down text-xs hidden lg:!block"></i>
                </summary>
                <div class="absolute right-0 mt-1 w-48 bg-white border border-gray-200 rounded-md shadow-lg z-50 hidden lg:group-open:block">
                    <a class="flex items-center gap-2 px-4 py-2 text-sm text-red-600 hover:bg-red-50 transition-colors" href="<?= route('users.logout') ?>">
                        <i class="bi bi-box-arrow-right text-xl text-red-600"></i>
                        Sair
                    </a>
                </div>
            </details>
        <?php endif; ?>
    </nav>

    <aside id="sidebar" class="fixed top-0 left-0 bottom-0 w-2/3 lg:w-64 z-50 bg-white shadow-2xl -translate-x-full transition-transform duration-300 hidden flex flex-col">
        <a href="<?= $this->current_user->is_admin ? route('admin.index') : route('root') ?>" class="h-15 flex items-center px-4 border-b border-black/10">
            <img src="/assets/images/defaults/logo.svg" class="h-8 w-auto" alt="Calorize">
        </a>

        <nav class="mt-1 flex-1 overflow-y-auto">
            <ul class="flex flex-col">
                <li>
                    <a class="flex items-center block gap-2 px-4 py-2 text-sm text-gray-700 hover:bg-gray-100" href="<?= route('root') ?>">
                        <i class="bi bi-house-door-fill text-xl text-black"></i>
                        <span class="font-medium">Início</span>
                    </a>
                </li>
                <li>
                    <a class="flex items-center block gap-2 px-4 py-2 text-sm text-gray-700 hover:bg-gray-100" href="<?= route('diets.index') ?>">
                        <i class="bi bi-table text-xl text-black"></i>
                        <span class="font-medium">Minhas dietas</span>
                    </a>
                </li>
                <li>
                    <a class="flex items-center block gap-2 px-4 py-2 text-sm text-gray-700 hover:bg-gray-100" href="<?= route('profile.foods.index') ?>">
                        <i class="bi bi-egg-fried text-xl text-black"></i>
                        <span class="font-medium">Meus Alimentos</span>
                    </a>
                </li>
                <li>
                    <a class="flex items-center block gap-2 px-4 py-2 text-sm text-gray-700 hover:bg-gray-100" href="<?= route('profile.show') ?>">
                        <i class="bi bi-person-fill text-xl text-black"></i>
                        <span class="font-medium">Perfil</span>
                    </a>
                </li>
            </ul>
        </nav>

        <div class="mt-auto border-t border-black/10 shrink-0">
            <a class="flex items-center block gap-2 px-4 py-2 text-sm text-red-600 hover:bg-red-50 transition-colors" href="<?= route('users.logout') ?>">
                <i class="bi bi-box-arrow-right text-xl text-red-600"></i>
                <span class="font-medium">Sair</span>
            </a>
        </div>
    </aside>

    <div id="sidebar-overlay" class="fixed inset-0 bg-black/40 backdrop-blur-sm z-40 hidden"></div>

    <main class="py-2 px-6 min-h-[calc(100vh-3.75rem)] flex flex-col">
        <div class="w-full lg:!w-3/4 mx-auto flex-1 flex flex-col">

            <?php require '_flash_message.phtml'; ?>

            <header class="mb-3 text-center">
                <?php if (!empty($title)) : ?>
                    <h1 class="text-2xl font-semibold text-gray-800">
                        <?= $title ?>
                    </h1>
                <?php endif; ?>
            </header>

            <?php require $view ?>
        </div>
    </main>

    <script src="/assets/js/sidebar.js"></script>

</body>

</html>