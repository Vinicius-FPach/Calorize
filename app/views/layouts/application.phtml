<html lang="pt-br">

<head>
    <?php require '_head.phtml'; ?>
    <title>Calorize</title>

    <script src="/assets/js/application.js"></script>

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
</head>

<body class="mt-15 bg-gray-50">
    <nav class="fixed top-0 left-0 right-0 z-50 bg-white border-b border-black/10 h-15 flex items-center px-4">
        <a class="text-lg font-semibold text-gray-800 hover:text-gray-600" href="<?= $this->current_user->is_admin ? route('admin.index') : route('problems.index') ?>">Calorize</a>

        <?php if ($this->current_user) : ?>
    <details class="ml-auto relative">
        <summary class="flex items-center gap-1 text-sm text-gray-700 hover:text-gray-900 cursor-pointer list-none">
            <?= $this->current_user->email ?>
            <i class="bi bi-chevron-down text-xs"></i>
        </summary>
        <div class="absolute right-0 mt-1 w-48 bg-white border border-gray-200 rounded-md shadow-lg z-50">
            <a class="block px-4 py-2 text-sm text-gray-700 hover:bg-gray-100" href="<?= route('profile.show') ?>">Profile</a>
            <a class="block px-4 py-2 text-sm text-gray-700 hover:bg-gray-100" href="<?= route('users.logout') ?>">Sair</a>
        </div>
    </details>
<?php endif; ?>
    </nav>

    <aside class="fixed top-15 left-0 bottom-0 w-64 z-40 bg-white border-r border-black/10">
        <nav class="mt-1">
            <ul class="flex flex-col">
                <li>
                    <a class="block px-4 py-2 text-sm text-gray-700 hover:bg-gray-100" href="<?= route('problems.index') ?>">Minhas dietas</a>
                </li>
                <li>
                    <a class="block px-4 py-2 text-sm text-gray-700 hover:bg-gray-100" href="<?= route('reinforce.problems.supported') ?>">Alimentos adicionados</a>
                </li>
                <li>
                    <a class="block px-4 py-2 text-sm text-gray-700 hover:bg-gray-100" href="<?= route('reinforce.problems') ?>">Todas as dietas</a>
                </li>
            </ul>
        </nav>
    </aside>

    <main class="ml-64 pt-2 px-6">

        <?php require '_flash_message.phtml'; ?>

        <header class="mb-3">
            <h1 class="text-2xl font-semibold text-gray-800">
                <?= $title ?>
            </h1>
        </header>

        <?php require $view ?>

    </main>

</body>

</html>