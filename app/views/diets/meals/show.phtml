<div class="flex items-center justify-between mb-4">
    <p class="text-sm text-gray-500">
        Refeição de <a href="<?= route('diets.show', ['id' => $diet->id]) ?>" class="text-primary font-semibold"><?= $diet->name ?></a>
    </p>

    <button onclick="openModal('delete-meal')" class="text-sm text-red-600 hover:underline cursor-pointer">
        <i class="bi bi-trash"></i> Remover refeição
    </button>
</div>

<div id="delete-meal" class="fixed inset-0 z-50 hidden items-center justify-center bg-black/50 backdrop-blur-sm transition-opacity">
    <div class="bg-white rounded-XL p-6 shadow-xl max-w-sm w-full mx-4 transform transition-all">
        <div class="flex items-center gap-3 mb-3 text-red-600">
            <i class="bi bi-exclamation-triangle text-xl"></i>
            <h3 class="text-lg font-bold text-gray-900">Excluir refeição</h3>
        </div>
        <p class="text-sm text-gray-500 mb-6 leading-relaxed">Tem certeza que deseja excluir <strong class="text-primary font-semibold"><?= $meal->name ?></strong> e todos os alimentos nela? Essa ação não pode ser desfeita.</p>
        <div class="flex justify-end gap-3">
            <button onclick="closeModal('delete-meal')" class="px-4 py-2 text-sm font-black text-gray-600 bg-gray-100 rounded-md hover:bg-gray-200 transition-colors cursor-pointer">
                CANCELAR
            </button>
            <form action="<?= route('meals.destroy', ['diet_id' => $diet->id, 'meal_id' => $meal->id]) ?>" method="POST" class="m-0">
                <input type="hidden" name="_method" value="DELETE">
                <button type="submit" class="px-4 py-2 text-sm font-black text-white bg-red-600 rounded-md hover:bg-red-700 shadow-sm shadow-red-200 transition-colors cursor-pointer">
                    EXCLUIR
                </button>
            </form>
        </div>
    </div>
</div>

<!-- Totais da refeição -->
<div class="bg-white border border-gray-200 rounded-lg p-6 mb-6">
    <h2 class="text-base font-semibold text-gray-800 mb-4">Total da refeição</h2>

    <div class="grid grid-cols-2 lg:grid-cols-4 gap-4">
        <div class="text-center p-3 bg-primary/10 border border-primary/20 rounded-lg">
            <p class="text-sm font-bold text-gray-700 mb-1">Calorias</p>
            <p class="text-lg font-black text-primary"><?= number_format($totals['kcal'], 2) ?></p>
            <p class="text-sm font-bold text-gray-500">kcal</p>
        </div>
        <div class="text-center p-3 bg-gray-50 border border-gray-200 rounded-lg">
            <p class="text-sm font-bold text-gray-700 mb-1">Proteína</p>
            <p class="text-lg font-semibold text-gray-800"><?= number_format($totals['protein'], 2) ?>g</p>
        </div>
        <div class="text-center p-3 bg-gray-50 border border-gray-200 rounded-lg">
            <p class="text-sm font-bold text-gray-700 mb-1">Carboidrato</p>
            <p class="text-lg font-semibold text-gray-800"><?= number_format($totals['carbs'], 2) ?>g</p>
        </div>
        <div class="text-center p-3 bg-gray-50 border border-gray-200 rounded-lg">
            <p class="text-sm font-bold text-gray-700 mb-1">Gordura</p>
            <p class="text-lg font-semibold text-gray-800"><?= number_format($totals['fats'], 2) ?>g</p>
        </div>
    </div>
</div>

<!-- Itens já adicionados -->
<div class="bg-white border border-gray-200 rounded-lg p-6 mb-6">
    <h2 class="text-base font-semibold text-gray-800 mb-4">Alimentos nesta refeição</h2>

    <?php if (empty($items)) : ?>
        <p class="text-sm text-gray-400">Nenhum alimento adicionado ainda.</p>
    <?php else : ?>
        <ul class="divide-y divide-gray-100">
            <?php foreach ($items as $item) : ?>
                <li class="flex items-center justify-between py-3">
                    <div>
                        <p class="text-sm font-semibold text-gray-800"><?= $item['food']->name ?></p>
                        <p class="text-xs text-gray-500">
                            <?= number_format($item['quantity'], 2) ?><?= $item['food']->unit ?>
                            &middot;
                            <?= number_format($item['food']->kcal * $item['quantity'] / 100, 2) ?> kcal
                        </p>
                    </div>

                    <button onclick="openModal('delete-item-<?= $item['food_meal_id'] ?>')" class="text-red-600 hover:text-red-800 cursor-pointer">
                        <i class="bi bi-x-lg"></i>
                    </button>
                </li>

                <div id="delete-item-<?= $item['food_meal_id'] ?>" class="fixed inset-0 z-50 hidden items-center justify-center bg-black/50 backdrop-blur-sm transition-opacity">
                    <div class="bg-white rounded-XL p-6 shadow-xl max-w-sm w-full mx-4 transform transition-all">
                        <div class="flex items-center gap-3 mb-3 text-red-600">
                            <i class="bi bi-exclamation-triangle text-xl"></i>
                            <h3 class="text-lg font-bold text-gray-900">Remover alimento</h3>
                        </div>
                        <p class="text-sm text-gray-500 mb-6 leading-relaxed">Remover <strong class="text-primary font-semibold"><?= $item['food']->name ?></strong> desta refeição?</p>
                        <div class="flex justify-end gap-3">
                            <button onclick="closeModal('delete-item-<?= $item['food_meal_id'] ?>')" class="px-4 py-2 text-sm font-black text-gray-600 bg-gray-100 rounded-md hover:bg-gray-200 transition-colors cursor-pointer">
                                CANCELAR
                            </button>
                            <form action="<?= route('food_meal.destroy', ['food_meal_id' => $item['food_meal_id']]) ?>" method="POST" class="m-0">
                                <input type="hidden" name="_method" value="DELETE">
                                <button type="submit" class="px-4 py-2 text-sm font-black text-white bg-red-600 rounded-md hover:bg-red-700 shadow-sm shadow-red-200 transition-colors cursor-pointer">
                                    REMOVER
                                </button>
                            </form>
                        </div>
                    </div>
                </div>
            <?php endforeach; ?>
        </ul>
    <?php endif; ?>
</div>

<div class="bg-white border border-gray-200 rounded-lg p-6 mb-6">
    <h2 class="text-base font-semibold text-gray-800 mb-4">Alimentos favoritos</h2>

    <?php if (empty($favorites)) : ?>
        <p class="text-sm text-gray-400">Nenhum alimento favorito adicionado ainda.</p>
    <?php else : ?>
        <ul class="divide-y divide-gray-100">
            <?php foreach ($favorites as $favorite) : ?>
                <li class="flex items-center justify-between py-3">
                    <div>
                        <p class="text-sm font-semibold text-gray-800"><?= $favorite['food']->name ?></p>
                        <p class="text-xs text-gray-500">
                            <?= number_format($favorite['quantity'], 2) ?><?= $favorite['food']->unit ?>
                            &middot;
                            <?= number_format($favorite['food']->kcal * $favorite['quantity'] / 100, 2) ?> kcal
                        </p>
                    </div>

                    <form action="<?= route('food_meal.favorite', ['food_meal_id' => $favorite['food_meal_id']]) ?>" method="POST" class="m-0">
                        <input type="hidden" name="_method" value="PUT">
                        <button type="submit" class="px-3 py-1 bg-red-800 text-white font-black text-xs rounded-md hover:opacity-90 cursor-pointer transition-colors">
                            <i class="bi bi-hand-thumbs-down-fill"></i>
                        </button>
                    </form>
                </li>
            <?php endforeach; ?>
        </ul>
    <?php endif; ?>
</div>

<!-- Busca e adição de alimentos -->
<div class="bg-white border border-gray-200 rounded-lg p-6">
    <h2 class="text-base font-semibold text-gray-800 mb-4">Adicionar alimento</h2>

    <form action="<?= route('meals.show', ['diet_id' => $diet->id, 'meal_id' => $meal->id]) ?>" method="GET" class="mb-4">
        <input type="text" name="search" value="<?= $search ?>" placeholder="Buscar alimento pelo nome..."
            class="w-1/2 bg-[#f6f6f6] border border-gray-200 rounded-md px-3 py-2 text-sm shadow-sm focus:outline-none focus:ring-2 focus:ring-primary">
        <button type="submit" class="px-4 py-2 bg-gray-100 text-gray-700 font-black text-sm rounded-md hover:bg-gray-200 transition-colors">
            <i class="bi bi-search"></i>
        </button>
    </form>

    <?php if (empty($availableFoods)) : ?>
        <p class="text-sm text-gray-400">Nenhum alimento encontrado.</p>
    <?php else : ?>
        <ul class="divide-y divide-gray-100">
            <?php foreach ($availableFoods as $food) : ?>
                <li class="flex items-center justify-between py-3 gap-4">
                    <div>
                        <p class="text-sm font-semibold text-gray-800"><?= $food->name ?></p>
                        <p class="text-xs text-gray-500"><?= $food->kcal ?> kcal por 100<?= $food->unit ?></p>
                    </div>

                    <form action="<?= route('meals.foods.add', ['diet_id' => $diet->id, 'meal_id' => $meal->id]) ?>" method="POST" class="flex items-center gap-2">
                        <input type="hidden" name="food_meal[food_id]" value="<?= $food->id ?>">
                        <input type="number" name="food_meal[quantity]" step="0.01" min="0.01" placeholder="<?= $food->unit ?>"
                            class="w-20 bg-[#f6f6f6] border border-gray-200 rounded-md px-2 py-1 text-sm shadow-sm focus:outline-none focus:ring-2 focus:ring-primary">
                        <button type="submit" class="px-3 py-1 bg-primary text-white font-black text-xs rounded-md hover:opacity-90 cursor-pointer transition-colors">
                            <i class="bi bi-plus"></i>
                        </button>
                    </form>

                    <form action="<?= route('meals.foods.addFavorite', ['diet_id' => $diet->id, 'meal_id' => $meal->id]) ?>" method="POST" class="flex items-center gap-2">
                        <input type="hidden" name="food_meal[food_id]" value="<?= $food->id ?>">
                        <input type="hidden" name="food_meal[quantity]" value="100">
                        <input type="hidden" name="food_meal[favorite]" value="1">
                        <button type="submit" class="px-3 py-1 bg-primary text-white font-black text-xs rounded-md hover:opacity-90 cursor-pointer transition-colors">
                            <i class="bi bi-hand-thumbs-up-fill"></i>
                        </button>
                    </form>
                </li>
            <?php endforeach; ?>
        </ul>
    <?php endif; ?>
</div>