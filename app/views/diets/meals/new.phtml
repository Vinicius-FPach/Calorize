<div class="bg-white border border-gray-200 rounded-lg p-6 mb-6">
    <h2 class="text-base font-semibold text-gray-800 mb-4">Nova refeição para "<?= $diet->name ?>"</h2>

    <form action="<?= route('meals.create', ['diet_id' => $diet->id]) ?>" method="POST" novalidate>
        <div class="mb-4">
            <label class="block text-sm font-black text-gray-700 mb-1">NOME DA REFEIÇÃO</label>
            <input type="text" name="meal[name]" value="<?= $meal->name ?>" placeholder="Ex: Almoço, Pré-Treino"
                class="w-1/2 bg-[#f6f6f6] border border-gray-200 rounded-md px-3 py-2 text-sm shadow-sm focus:outline-none focus:ring-2 focus:ring-primary">
            <?php if ($meal->errors('name')) : ?>
                <p class="text-red-600 text-xs mt-1"><?= $meal->errors('name') ?></p>
            <?php endif; ?>
        </div>

        <div class="flex gap-3">
            <button type="submit" class="px-6 py-2 bg-primary text-white font-black text-sm rounded-md hover:opacity-90 cursor-pointer transition-colors">
                <i class="bi bi-plus font-black"></i>
                CRIAR REFEIÇÃO
            </button>

            <a href="<?= route('diets.show', ['id' => $diet->id]) ?>"
                class="px-6 py-2 bg-gray-100 text-gray-700 font-black text-sm rounded-md hover:bg-gray-200 transition-colors">
                CANCELAR
            </a>
        </div>
    </form>
</div>