<div class="flex justify-start mb-4">
    <a href="<?= route('diets.new') ?>" class="flex items-center font-black gap-1 px-4 py-2 text-sm text-white rounded-md bg-primary hover:opacity-90">
        <i class="bi bi-plus font-black"></i>
        NOVA DIETA
    </a>
</div>

<?php if (empty($diets)) : ?>
    <p class="flex justify-center text-sm text-gray-500">Nenhuma dieta registrada ainda.</p>
<?php else : ?>
    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4">
        <?php foreach ($diets as $diet) : ?>
            <div class="bg-white border border-gray-200 rounded-lg p-4 flex flex-col gap-3 shadow-sm">
                <div>
                    <h1 class="text-xl font-semibold text-gray-800"><?= $diet->name ?></h1>
                    <div class="text-center p-3 bg-primary/10 border border-primary/20 rounded-lg">
                        <p class="text-sm font-bold text-gray-700 mb-1">Meta Calórica</p>
                        <p class="text-lg font-black text-primary"><?= $diet->kcal_objt ?></p>
                        <p class="text-sm font-bold text-gray-500">kcal/dia</p>
                    </div>
                </div>
                <div class="flex justify-between mt-auto">
                    <a href="<?= route('diets.show', ['id' => $diet->id]) ?>" class="p-1.5 text-gray-500 hover:text-primary transition-colors">
                        <i class="bi bi-eye font-black"></i>
                    </a>
                    <div class="flex gap-2">
                        <a href="<?= route('diets.edit', ['id' => $diet->id]) ?>" class="p-1.5 text-gray-500 hover:text-primary transition-colors">
                            <i class="bi bi-pencil-square"></i>
                        </a>
                        <button onclick="openModal('delete-<?= $diet->id ?>')" class="p-1.5 text-gray-500 hover:text-red-600 transition-colors cursor-pointer">
                            <i class="bi bi-trash"></i>
                        </button>
                    </div>
                </div>
            </div>

            <div id="delete-<?= $diet->id ?>" class="fixed inset-0 z-50 hidden items-center justify-center bg-black/40">
                <div class="bg-white rounded-lg p-6 shadow-xl max-w-sm w-full mx-4">
                    <h3 class="text-base font-semibold text-gray-800 mb-2">Excluir dieta</h3>
                    <p class="text-sm text-gray-500 mb-6">Tem certeza que deseja excluir <strong class="text-primary"><?= $diet->name ?></strong>? Essa ação não pode ser desfeita.</p>
                    <div class="flex justify-end gap-3">
                        <button onclick="closeModal('delete-<?= $diet->id ?>')" class="px-4 py-2 text-sm font-black text-gray-600 border border-gray-300 rounded-md hover:bg-gray-50 cursor-pointer">
                            CANCELAR
                        </button>
                        <form action="<?= route('diets.destroy', ['id' => $diet->id]) ?>" method="POST" class="m-0">
                            <input type="hidden" name="_method" value="DELETE">
                            <button type="submit" class="px-4 py-2 text-sm font-black text-white bg-red-600 rounded-md hover:bg-red-700 cursor-pointer">
                                EXCLUIR
                            </button>
                        </form>
                    </div>
                </div>
            </div>
        <?php endforeach; ?>
    </div>
<?php endif; ?>

<?php $paginator->renderPagesNavigation(); ?>

<script src="/assets/js/modal.js"></script>