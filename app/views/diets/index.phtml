<div class="flex justify-center mb-4">
    <a href="<?= route('diets.new') ?>" class="flex items-center gap-1 px-4 py-2 text-sm text-white rounded-md hover:opacity-90" style="background-color: #098A26">
        <i class="bi bi-plus"></i>
        Nova Dieta
    </a>
</div>

<?php if (empty($diets)) : ?>
    <p class="flex justify-center text-sm text-gray-500">Nenhuma dieta registrada ainda.</p>
<?php else : ?>
    <div class="flex flex-col gap-3">
        <?php foreach ($diets as $diet) : ?>
            <div class="flex items-center justify-between bg-white border border-gray-200 rounded-lg px-4 py-3">
                <span class="text-sm font-medium text-gray-800"><?= $diet->name ?></span>
                <form action="<?= route('diets.destroy', ['id' => $diet->id]) ?>" method="POST" class="m-0">
                    <input type="hidden" name="_method" value="DELETE">
                    <button type="submit" class="p-1 text-gray-400 hover:text-red-600 cursor-pointer">
                        <i class="bi bi-trash"></i>
                    </button>
                </form>
            </div>
        <?php endforeach; ?>
    </div>
<?php endif; ?>