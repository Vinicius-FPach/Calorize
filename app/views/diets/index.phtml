<div class="flex justify-start mb-6">
    <a href="<?= route('diets.new') ?>" class="flex items-center font-semibold gap-1.5 px-4 py-2.5 text-sm text-white rounded-lg bg-primary hover:bg-primary/90 shadow-sm transition-all duration-200">
        <i class="bi bi-plus-lg text-base"></i>
        NOVA DIETA
    </a>
</div>

<?php if (empty($diets)) : ?>
    <p class="flex justify-center text-sm text-gray-500 py-12 border border-dashed border-gray-200 rounded-xl">Nenhuma dieta registrada ainda.</p>
<?php else : ?>
    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4">
        <?php foreach ($diets as $diet) : ?>
            <div onclick="window.location.href='<?= route('diets.show', ['id' => $diet->id]) ?>'"
                class="relative bg-white border border-gray-100 rounded-xl p-5 py-7 shadow-sm hover:shadow-md transition-all duration-200 cursor-pointer flex flex-col">

                <div class="flex items-baseline gap-1.5 mt-auto">
                    <span class="text-3xl font-black text-primary leading-none"><?= $diet->totals()['kcal'] ?></span>
                </div>
                <div class="flex items-center justify-between gap-2 mb-5">
                    <div class="flex items-center gap-3 min-w-0">
                        <div class="w-10 h-10 flex items-center justify-center bg-primary/10 rounded-full flex-shrink-0">
                            <i class="bi bi-egg-fried text-primary text-lg"></i>
                        </div>
                        <h1 class="text-lg font-bold text-gray-800 leading-tight line-clamp-1"><?= $diet->name ?></h1>
                    </div>

                    <div class="flex gap-1 flex-shrink-0">
                        <a href="<?= route('diets.edit', ['id' => $diet->id]) ?>"
                            onclick="event.stopPropagation()"
                            class="p-1.5 text-gray-400 hover:text-primary hover:bg-gray-50 rounded-md transition-colors" title="Editar">
                            <i class="bi bi-pencil-square text-sm flex"></i>
                        </a>
                        <button onclick="event.stopPropagation(); openModal('delete-<?= $diet->id ?>')"
                            class="p-1.5 text-gray-400 hover:text-red-600 hover:bg-gray-50 rounded-md transition-colors cursor-pointer" title="Excluir">
                            <i class="bi bi-trash text-sm flex"></i>
                        </button>
                    </div>
                </div>

                <div class="flex items-baseline gap-1.5 mt-auto">
                    <span class="text-3xl font-black text-primary leading-none"><?= $diet->kcal_objt ?></span>
                    <span class="text-xs font-bold text-gray-400">kcal/dia</span>
                </div>
            </div>

            <div id="delete-<?= $diet->id ?>" class="fixed inset-0 z-50 hidden items-center justify-center bg-black/50 backdrop-blur-sm transition-opacity">
                <div class="bg-white rounded-XL p-6 shadow-xl max-w-sm w-full mx-4 transform transition-all">
                    <div class="flex items-center gap-3 mb-3 text-red-600">
                        <i class="bi bi-exclamation-triangle text-xl"></i>
                        <h3 class="text-lg font-bold text-gray-900">Excluir dieta</h3>
                    </div>
                    <p class="text-sm text-gray-500 mb-6 leading-relaxed">Tem certeza que deseja excluir <strong class="text-primary font-semibold"><?= $diet->name ?></strong>? Essa ação não pode ser desfeita.</p>
                    <div class="flex justify-end gap-3">
                        <button onclick="closeModal('delete-<?= $diet->id ?>')" class="px-4 py-2 text-sm font-black text-gray-600 bg-gray-100 rounded-md hover:bg-gray-200 transition-colors cursor-pointer">
                            CANCELAR
                        </button>
                        <form action="<?= route('diets.destroy', ['id' => $diet->id]) ?>" method="POST" class="m-0">
                            <input type="hidden" name="_method" value="DELETE">
                            <button type="submit" class="px-4 py-2 text-sm font-black text-white bg-red-600 rounded-md hover:bg-red-700 shadow-sm shadow-red-200 transition-colors cursor-pointer">
                                EXCLUIR
                            </button>
                        </form>
                    </div>
                </div>
            </div>
        <?php endforeach; ?>
    </div>
<?php endif; ?>

<?php $paginator->renderPagesNavigation(); ?>