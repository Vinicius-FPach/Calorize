<div class="flex justify-start mb-6">
    <a href="<?= route('profile.foods.new') ?>" class="flex items-center font-semibold gap-1.5 px-4 py-2.5 text-sm text-white rounded-lg bg-primary hover:bg-primary/90 shadow-sm transition-all duration-200">
        <i class="bi bi-plus-lg text-base"></i>
        NOVO ALIMENTO
    </a>
</div>

<?php if (empty($foods)) : ?>
    <p class="flex justify-center text-sm text-gray-500 py-12 border border-dashed border-gray-200 rounded-xl">Nenhum alimento registrado ainda.</p>
<?php else : ?>
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-5">
        <?php foreach ($foods as $food) : ?>
            <div class="bg-white border border-gray-100 rounded-xl p-4 flex gap-4 shadow-sm hover:shadow-md transition-all duration-200 items-center">
                
                <div class="w-36 h-36 rounded-lg overflow-hidden flex-shrink-0 border border-black/5 <?= $food->photo_url ? '' : 'bg-gray-50 flex items-center justify-center' ?>">
                    <img src="<?= $food->image()->path() ?>"
                        alt="<?= $food->name ?>"
                        class="<?= $food->photo_url
                            ? 'w-36 h-36 object-cover'
                            : 'w-16 h-16 object-contain opacity-50' ?>">
                </div>
                
                <div class="flex-1 flex flex-col justify-between h-36 py-0.5">
                    <div class="flex justify-between items-start gap-2">
                        <div>
                            <h1 class="text-base font-bold text-gray-800 line-clamp-1"><?= $food->name ?></h1>
                            <p class="text-xs font-medium text-gray-400 mt-0.5"><?= $food->category ?></p>
                        </div>
                        
                        <div class="flex gap-0.5 bg-gray-50 p-1 rounded-lg flex-shrink-0">
                            <a href="<?= route('profile.foods.show', ['uuid' => $food->uuid]) ?>" class="p-1 text-gray-400 hover:text-primary rounded transition-colors" title="Visualizar">
                                <i class="bi bi-eye text-base flex"></i>
                            </a>
                            <a href="<?= route('profile.foods.edit', ['uuid' => $food->uuid]) ?>" class="p-1 text-gray-400 hover:text-primary rounded transition-colors" title="Editar">
                                <i class="bi bi-pencil-square text-base flex"></i>
                            </a>
                            <button type='submit' class="p-1 text-gray-400 hover:text-red-600 rounded transition-colors cursor-pointer" title="Excluir">
                                <i class="bi bi-trash text-base flex"></i>
                            </button>
                            <button onclick="openModal('delete-<?= $food->id ?>')" class="p-1 text-gray-400 hover:text-red-600 rounded transition-colors cursor-pointer" title="Excluir">
                                <i class="bi bi-trash text-base flex"></i>
                            </button>
                        </div>
                    </div>
                    
                    <div class="flex flex-col gap-2">
                        <div class="flex items-center justify-between px-3 py-1.5 bg-primary/5 border border-primary/10 rounded-lg">
                            <span class="text-xs font-bold text-primary tracking-wide uppercase">Calorias</span>
                            <div class="flex items-baseline gap-1">
                                <span class="text-xl font-black text-primary leading-none"><?= $food->kcal ?></span>
                                <span class="text-[11px] font-medium text-gray-400">kcal / 100<?= $food->unit ?></span>
                            </div>
                        </div>

                        <div class="grid grid-cols-3 gap-1.5 text-center bg-gray-50 rounded-lg p-1.5 border border-gray-100">
                            <div>
                                <p class="text-[10px] font-bold text-gray-400 uppercase">Prot</p>
                                <p class="text-xs font-bold text-gray-700 mt-0.5"><?= $food->protein ?? '0' ?><span class="text-[10px] font-normal text-gray-400 ml-0.5">g</span></p>
                            </div>
                            <div class="border-x border-gray-200">
                                <p class="text-[10px] font-bold text-gray-400 uppercase">Carb</p>
                                <p class="text-xs font-bold text-gray-700 mt-0.5"><?= $food->carbs ?? '0' ?><span class="text-[10px] font-normal text-gray-400 ml-0.5">g</span></p>
                            </div>
                            <div>
                                <p class="text-[10px] font-bold text-gray-400 uppercase">Gord</p>
                                <p class="text-xs font-bold text-gray-700 mt-0.5"><?= $food->fats ?? '0' ?><span class="text-[10px] font-normal text-gray-400 ml-0.5">g</span></p>
                            </div>
                        </div>
                    </div>

                </div>
            </div>

            <div id="delete-<?= $food->id ?>" class="fixed inset-0 z-50 hidden items-center justify-center bg-black/50 backdrop-blur-sm transition-opacity">
                <div class="bg-white rounded-xl p-6 shadow-xl max-w-sm w-full mx-4 transform transition-all">
                    <div class="flex items-center gap-3 mb-3 text-red-600">
                        <i class="bi bi-exclamation-triangle text-xl"></i>
                        <h3 class="text-lg font-bold text-gray-900">Excluir alimento</h3>
                    </div>
                    <p class="text-sm text-gray-500 mb-6 leading-relaxed">Tem certeza que deseja excluir <strong class="text-primary font-semibold"><?= $food->name ?></strong>? Essa ação não pode ser desfeita.</p>
                    <div class="flex justify-end gap-3">
                        <button onclick="closeModal('delete-<?= $food->id ?>')" class="px-4 py-2 text-sm font-black text-gray-600 bg-gray-100 rounded-md hover:bg-gray-200 transition-colors cursor-pointer">
                            CANCELAR
                        </button>
                        <form action="<?= route('profile.foods.destroy', ['uuid' => $food->uuid]) ?>" method="POST" class="m-0">
                            <input type="hidden" name="_method" value="DELETE">
                            <button type="submit" class="px-4 py-2 text-sm font-black text-white bg-red-600 rounded-md hover:bg-red-700 shadow-sm shadow-red-200 transition-colors cursor-pointer">
                                EXCLUIR
                            </button>
                        </form>
                    </div>
                </div>
            </div>
        <?php endforeach; ?>
    </div>
<?php endif; ?>

<div class="mt-6">
    <?php $paginator->renderPagesNavigation(); ?>
</div>