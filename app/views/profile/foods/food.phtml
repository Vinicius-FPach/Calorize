<?php $currentUnit = ($food->unit === 'ml') ? 'ml' : 'g'; ?>
<div class="flex justify-center pt-10">
    <form action="<?= $food->id ? route('profile.foods.update', ['uuid' => $food->uuid]) : route('profile.foods.create') ?>" method="POST" enctype="multipart/form-data" novalidate class="w-full lg:!w-3/4">
        <?php if ($food->id) : ?>
            <input type="hidden" name="_method" value="PUT">
        <?php endif; ?>

        <div class="grid grid-cols-1 md:grid-cols-2 gap-4 mb-4">
            <div>
                <label class="block text-xs font-black uppercase tracking-wider text-gray-700 mb-1.5">NOME</label>
                <input type="text" name="food[name]" value="<?= $food->name ?>" placeholder="Ex: Peito de Frango Grelhado"
                    class="w-full bg-gray-50 border border-gray-200 rounded-lg px-3 py-2 text-sm shadow-sm focus:outline-none focus:ring-2 focus:ring-primary focus:border-transparent transition-all">
                <?php if ($food->errors('name')) : ?>
                    <p class="text-red-600 text-xs mt-1 flex items-center gap-1">
                    <i class="bi bi-exclamation-circle"></i>
                    <?= $food->errors('name') ?></p>
                <?php endif; ?>
            </div>

            <div>
                <label class="block text-xs font-black uppercase tracking-wider text-gray-700 mb-1.5">CATEGORIA</label>
                <input type="text" name="food[category]" value="<?= $food->category ?>" placeholder="Ex: Frutas, Carnes"
                    class="w-full bg-gray-50 border border-gray-200 rounded-lg px-3 py-2 text-sm shadow-sm focus:outline-none focus:ring-2 focus:ring-primary focus:border-transparent transition-all">
                <?php if ($food->errors('category')) : ?>
                    <p class="text-red-600 text-xs mt-1 flex items-center gap-1">
                    <i class="bi bi-exclamation-circle"></i>
                    <?= $food->errors('category') ?></p>
                <?php endif; ?>
            </div>

            <div class="md:col-span-2">
                <label class="block text-xs font-black uppercase tracking-wider text-gray-700 mb-1.5">UNIDADE DE MEDIDA</label>
                <select id="unit-select" name="food[unit]" class="w-full bg-gray-50 border border-gray-200 rounded-lg pl-3 pr-14 py-2 text-sm shadow-sm focus:outline-none focus:ring-2 focus:ring-primary focus:border-transparent transition-all">
                    <option value="">Selecione</option>
                    <option value="g" <?= $food->unit === 'g' ? 'selected' : '' ?>>Gramas (g)</option>
                    <option value="ml" <?= $food->unit === 'ml' ? 'selected' : '' ?>>Mililitros (ml)</option>
                </select>
                <p class="text-xs text-gray-500 mt-2 flex flex-col sm:flex-row items-start sm:items-center gap-1.5 leading-relaxed sm:leading-none">
                    <span class="flex items-center gap-1 text-primary shrink-0">
                        <i class="bi bi-info-circle-fill"></i>
                        <strong>Atenção:</strong>
                    </span>
                    <span>
                        <strong class="hidden sm:inline">Atenção:</strong> 
                        Os valores nutricionais abaixo devem se referir estritamente à porção de <strong>100g ou 100ml</strong>.
                    </span>
                </p>
                <?php if ($food->errors('unit')) : ?>
                    <p class="text-red-600 text-xs mt-1 flex items-center gap-1">
                    <i class="bi bi-exclamation-circle"></i>
                    <?= $food->errors('unit') ?></p>
                <?php endif; ?>
            </div>

            <div>
                <label class="block text-xs font-black uppercase tracking-wider text-gray-700 mb-1.5">CALORIAS <span class="text-gray-400 font-normal">(por 100<span class="dynamic-unit"><?= $currentUnit ?></span>)</span></label>
                <div class="relative flex items-center">
                    <input type="number" step="1" min="0" max="900" name="food[kcal]" value="<?= $food->kcal ?>"
                        class="w-full bg-gray-50 border border-gray-200 rounded-lg pl-3 pr-14 py-2 text-sm shadow-sm focus:outline-none focus:ring-2 focus:ring-primary focus:border-transparent transition-all">
                    <span class="absolute right-3 text-xs font-semibold text-gray-400 pointer-events-none">kcal</span>
                </div>
                <?php if ($food->errors('kcal')) : ?>
                    <p class="text-red-600 text-xs mt-1 flex items-center gap-1">
                    <i class="bi bi-exclamation-circle"></i>
                    <?= $food->errors('kcal') ?></p>
                <?php endif; ?>
            </div>

            <div>
                <label class="block text-xs font-bold uppercase tracking-wider text-gray-700 mb-1.5">Proteínas <span class="text-gray-400 font-normal">(por 100<span class="dynamic-unit"><?= $currentUnit ?></span>)</span></label>
                <div class="relative flex items-center">
                    <input type="number" step="1" min="0" max="100" name="food[protein]" value="<?= $food->protein ?>"
                        class="w-full bg-gray-50 border border-gray-200 rounded-lg pl-3 pr-8 py-2 text-sm shadow-sm focus:outline-none focus:ring-2 focus:ring-primary focus:border-transparent transition-all">
                    <span class="absolute right-3 text-xs font-semibold text-gray-400 pointer-events-none dynamic-badge"><?= $currentUnit ?></span>
                </div>
                <?php if ($food->errors('protein')) : ?>
                    <p class="text-red-600 text-xs mt-1 flex items-center gap-1">
                    <i class="bi bi-exclamation-circle"></i>
                    <?= $food->errors('protein') ?></p>
                <?php endif; ?>
            </div>

            <div>
                <label class="block text-xs font-bold uppercase tracking-wider text-gray-700 mb-1.5">Carboidratos <span class="text-gray-400 font-normal">(por 100<span class="dynamic-unit"><?= $currentUnit ?></span>)</span></label>
                <div class="relative flex items-center">
                    <input type="number" step="1" min="0" max="100" name="food[carbs]" value="<?= $food->carbs ?>"
                        class="w-full bg-gray-50 border border-gray-200 rounded-lg pl-3 pr-8 py-2 text-sm shadow-sm focus:outline-none focus:ring-2 focus:ring-primary focus:border-transparent transition-all">
                    <span class="absolute right-3 text-xs font-semibold text-gray-400 pointer-events-none dynamic-badge"><?= $currentUnit ?></span>
                </div>
                <?php if ($food->errors('carbs')) : ?>
                    <p class="text-red-600 text-xs mt-1 flex items-center gap-1">
                    <i class="bi bi-exclamation-circle"></i>
                    <?= $food->errors('carbs') ?></p>
                <?php endif; ?>
            </div>

            <div>
                <label class="block text-xs font-bold uppercase tracking-wider text-gray-700 mb-1.5">Gorduras <span class="text-gray-400 font-normal">(por 100<span class="dynamic-unit"><?= $currentUnit ?></span>)</span></label>
                <div class="relative flex items-center">
                    <input type="number" step="1" min="0" max="100" name="food[fats]" value="<?= $food->fats ?>"
                        class="w-full bg-gray-50 border border-gray-200 rounded-lg pl-3 pr-8 py-2 text-sm shadow-sm focus:outline-none focus:ring-2 focus:ring-primary focus:border-transparent transition-all">
                    <span class="absolute right-3 text-xs font-semibold text-gray-400 pointer-events-none dynamic-badge"><?= $currentUnit ?></span>
                </div>
                <?php if ($food->errors('fats')) : ?>
                    <p class="text-red-600 text-xs mt-1 flex items-center gap-1">
                    <i class="bi bi-exclamation-circle"></i>
                    <?= $food->errors('fats') ?></p>
                <?php endif; ?>
            </div>

            <div class="md:col-span-2">
                <div class="flex flex-col sm:flex-row items-start gap-4">
                    
                    <div class="w-32 h-32 rounded-xl overflow-hidden bg-gray-100 border border-gray-200 shadow-sm shrink-0 flex items-center justify-center">
                        <img id="image_preview"
                            src="<?= $food->image()->path() ?>"
                            data-default-image="<?= $food->image()->path()?>"
                            alt="Preview do alimento"
                            class="w-full h-full object-cover">
                    </div>
                    
                    <div class="flex-1 w-full flex flex-col justify-center gap-2">
                        <label class="block text-xs font-bold uppercase tracking-wider text-gray-700">
                            Foto do Alimento <span class="text-gray-400 font-normal text-xs transform none">(Opcional)</span>
                        </label>
                        
                        <input type="file" id="food_image_input" name="food_image" accept="image/jpeg,image/png"
                            class="w-full bg-gray-50 border border-gray-200 rounded-lg px-3 py-2 text-sm shadow-sm file:mr-3 file:py-1 file:px-3 file:rounded-md file:border-0 file:text-xs file:font-semibold file:bg-primary/10 file:text-primary hover:file:bg-primary/20 file:transition-colors focus:outline-none cursor-pointer">
                        <button
                            type="button"
                            id="remove-image-btn"
                            class="hidden items-center gap-2 px-3 py-2 text-sm font-medium text-red-600 border border-red-200 bg-red-50 hover:bg-red-100 rounded-lg transition-colors cursor-pointer">
                            Remover seleção
                        </button>
                        
                        <?php if ($food->photo_url) : ?>
                         <label class="flex items-center gap-2 text-sm text-red-600">
                         <input type="checkbox" name="remove_image" value="1">
                             Remover imagem atual
                         </label>
                        <?php endif; ?>

                        <?php if ($food->errors('imageFile')) : ?>
                            <p class="text-red-600 text-xs mt-1 flex items-center gap-1">
                                <i class="bi bi-exclamation-circle"></i>
                                <?= $food->errors('imageFile') ?>
                            </p>
                        <?php endif; ?>
                    </div>
                    
                </div>
            </div>
        </div>

        <button type="submit" class="flex items-center gap-1.5 px-6 py-2 bg-primary text-white font-bold text-sm rounded-md hover:opacity-90 shadow-sm transition-all duration-200 cursor-pointer">
            <i class="bi bi-plus text-base"></i>
            <?= $food->id ? 'ATUALIZAR ALIMENTO' : 'CRIAR ALIMENTO' ?>
        </button>
    </form>
</div>