<div class="flex justify-between items-center mb-6">
    <h1 class="text-xl font-bold text-gray-800">Alimentos Favoritos</h1>

    <a href="<?= route('profile.foods.index') ?>"
       class="text-sm text-primary font-semibold">
        Voltar
    </a>
</div>

<?php if (empty($foods)) : ?>
    <p class="text-center text-sm text-gray-500 py-12 border border-dashed rounded-xl">
        Nenhum alimento favoritado ainda.
    </p>
<?php else : ?>
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-5">
        <?php foreach ($foods as $food) : ?>
            <div class="bg-white border rounded-xl p-4 flex gap-4">

                <div class="w-24 h-24 rounded overflow-hidden border">
                    <img src="<?= $food->image()->path() ?>"
                         class="w-full h-full object-cover">
                </div>

                <div class="flex-1">
                    <h2 class="font-bold text-gray-800"><?= $food->name ?></h2>
                    <p class="text-sm text-gray-400"><?= $food->category ?></p>

                    <p class="text-sm mt-2">
                        <?= $food->kcal ?> kcal
                    </p>
                </div>

            </div>
        <?php endforeach; ?>
    </div>
<?php endif; ?>