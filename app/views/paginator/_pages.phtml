<nav class="flex flex-col items-end gap-1 mt-4">
    <ul class="flex gap-1">

        <?php if ($paginator->hasPreviousPage()) : ?>
            <li>
                <a class="px-3 py-1 text-sm border border-gray-300 rounded-md text-gray-700 hover:bg-gray-100"
                    href="<?= route($paginator->getRouteName(), ['page' => $paginator->previousPage()]) ?>">Anterior</a>
            </li>
        <?php else : ?>
            <li>
                <span class="px-3 py-1 text-sm border border-gray-200 rounded-md text-gray-400 cursor-not-allowed">Anterior</span>
            </li>
        <?php endif; ?>

        <?php for ($page = 1; $page <= $paginator->totalOfPages(); $page++) : ?>
            <li>
                <a class="px-3 py-1 text-sm border rounded-md <?= $paginator->isPage($page) ? 'bg-primary text-white border-primary' : 'border-gray-300 text-gray-700 hover:bg-gray-100' ?>"
                    href="<?= route($paginator->getRouteName(), ['page' => $page]) ?>"><?= $page ?></a>
            </li>
        <?php endfor; ?>

        <?php if ($paginator->hasNextPage()) : ?>
            <li>
                <a class="px-3 py-1 text-sm border border-gray-300 rounded-md text-gray-700 hover:bg-gray-100"
                    href="<?= route($paginator->getRouteName(), ['page' => $paginator->nextPage()]) ?>">Próxima</a>
            </li>
        <?php else : ?>
            <li>
                <span class="px-3 py-1 text-sm border border-gray-200 rounded-md text-gray-400 cursor-not-allowed">Próxima</span>
            </li>
        <?php endif; ?>

    </ul>

    <span class="text-xs text-gray-500"><?= $paginator->entriesInfo() ?></span>
</nav>